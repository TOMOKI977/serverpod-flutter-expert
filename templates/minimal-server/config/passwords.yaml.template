# Copy this file to passwords.yaml and fill in your values.
# NEVER commit passwords.yaml to version control.
#
#   cp config/passwords.yaml.template config/passwords.yaml

database:
  password: REPLACE_WITH_YOUR_DB_PASSWORD

serviceSecret: REPLACE_WITH_A_LONG_RANDOM_SECRET
